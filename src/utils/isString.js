/**
 * Tests whether or not an object is a string.
 * @param {*} object The object to test
 * @return {boolean} True if the object is a string, false otherwise.
 */
export default object => typeof object === 'string';